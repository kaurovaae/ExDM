import React                    from "react";

const LayoutBundle: React.FC = (): React.ReactElement => {
    return (
        <div>
            layout
        </div>
    )
}

export default LayoutBundle;
